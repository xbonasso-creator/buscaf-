[build]
  command   = "npx expo export --platform web"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA routing — todas las rutas apuntan a index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
